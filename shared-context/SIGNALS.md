# SIGNALS.md

- CEO/worker delegation is the latency unlock.
- File-based memory compounding is the quality moat.
- Session hygiene (idle reset) prevents degradation.
- Observability (traces/logs) prevents blind tuning.
